% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_stats.R
\name{plot_patient_counts_over_time}
\alias{plot_patient_counts_over_time}
\title{Plot Patient Counts Over Time}
\usage{
plot_patient_counts_over_time(
  data,
  title = "Patient Counts Over Time",
  year_range = NULL,
  output_path = NULL,
  save_plots = FALSE,
  auto_breaks = FALSE,
  log_scale = FALSE
)
}
\arguments{
\item{data}{A data frame from `output_data$combined` or a single code element from `extract_patient_counts_over_years()`.}

\item{title}{Title of the plot.}

\item{year_range}{Optional. A numeric vector of length 2 (e.g., `c(2000, 2020)`) to limit the x-axis range.}

\item{output_path}{Directory to save the plot if `save_plots = TRUE`. Defaults to `NULL`.}

\item{save_plots}{Logical; if `TRUE`, saves the plot to `output_path` as PNG.}

\item{auto_breaks}{Logical; if `TRUE`, enables non-uniform y-axis breaks based on patient count ranges.}

\item{log_scale}{Logical; if `TRUE`, applies log10 transformation to y-axis.}
}
\value{
A ggplot object.
}
\description{
Generates a line plot showing patient counts per year for one or more codes. The input `data` should come from 
`extract_patient_counts_over_years()`. You can apply linear or log scale and filter to a specific year range.
}
